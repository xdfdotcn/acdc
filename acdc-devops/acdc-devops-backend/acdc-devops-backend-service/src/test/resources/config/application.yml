# ===================================================================
# Spring Boot configuration.
#
# ===================================================================

spring:
  profiles:
  # Uncomment the following line to enable tests against production database type rather than H2, using Testcontainers
  #active: testcontainers
  application:
    name: acdcDevops
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:h2:mem:acdcdevops;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    name:
    username:
    password:
    hikari:
      auto-commit: false
  jackson:
    serialization:
      write-durations-as-timestamps: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
    properties:
      hibernate.format_sql: true
      hibernate.id.new_generator_mappings: true
      hibernate.connection.provider_disables_autocommit: true
      hibernate.cache.use_second_level_cache: false
      hibernate.cache.use_query_cache: false
      hibernate.generate_statistics: false
      hibernate.hbm2ddl.auto: validate
      hibernate.jdbc.time_zone: UTC
      hibernate.query.fail_on_pagination_over_collection_fetch: true
  liquibase:
    # Remove 'faker' if you do not want the sample data to be loaded automatically
    contexts: none
  mail:
    host: localhost
  main:
    allow-bean-definition-overriding: true
  messages:
    basename: i18n/messages
  thymeleaf:
    node: HTML

server:
  port: 10344
  address: localhost

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: trace

# application:

acdc:
  rdb:
    jdbc:
      user: root
      password: irDIaBmO3RhT
  hive:
    jdbc:
      url: jdbc:mysql://fake-mysql-host:3306/hive
      user: fake-user
      password: fake-password
  connector:
    topic:
      data-collection:
        partitions: 3
        replication-factor: 3
      schema-history:
        partitions: 1
        replication-factor: 3
        configs:
          cleanup.policy: delete
          retention.ms: 9223372036854775807
          retention.bytes: -1
      schema-change:
        partitions: 1
        replication-factor: 3
        configs:
          cleConnectionRepositorynup.policy: compact
          compression.type: lz4
      ticdc:
        partitions: 12
        replication-factor: 6
        acl:
          username: acdc
          operations:
            - READ
            - WRITE
  runtime:
    host:
      ranges:
        - '%'
        - '192.%'
        - '192.168.%'
        - '192.168.1.%'
      ips:
        - 192.168.1.2
        - 192.168.1.3
